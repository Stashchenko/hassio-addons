#!/usr/bin/with-contenv bashio
# ==============================================================================
# Start D-Bus daemon
# ==============================================================================

bashio::log.info "Starting dbus-daemon..."

# Ensure the runtime directory exists right before launching
mkdir -p /run/dbus

# Exec the daemon cleanly using the system configuration
exec dbus-daemon --system --nofork --nopidfile
